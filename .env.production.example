# =============================================================================
# RepoGuard AI — Production Environment
# =============================================================================
# Copy this file to .env.production and fill in all required secrets.
# NEVER commit .env.production to version control.
#
# Usage:
#   cp .env.production.example .env.production
#   docker compose --env-file .env.production -f docker-compose.prod.yml up -d --build
# =============================================================================

# ─── Application ──────────────────────────────────────────────────────────────
APP_NAME=RepoGuard AI
ENVIRONMENT=production
DEBUG=false
API_PREFIX=/api

# ─── Nginx (public port) ──────────────────────────────────────────────────────
# The only port exposed to the internet.
NGINX_HOST_PORT=80

# ─── PostgreSQL ───────────────────────────────────────────────────────────────
# Internal Docker service hostname: postgres
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DB=repoguard_ai
POSTGRES_USER=repoguard
POSTGRES_PASSWORD=CHANGE_ME_STRONG_PASSWORD

# ─── Redis ────────────────────────────────────────────────────────────────────
# Internal Docker service hostname: redis
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_DB=0
REDIS_PASSWORD=
REDIS_MAX_CONNECTIONS=10
REDIS_SOCKET_CONNECT_TIMEOUT_SECONDS=5.0
REDIS_SOCKET_TIMEOUT_SECONDS=5.0
REDIS_HEALTH_CHECK_INTERVAL_SECONDS=30

# ─── Celery ───────────────────────────────────────────────────────────────────
# Broker and result backend use the internal Redis service.
CELERY_BROKER_URL=redis://redis:6379/0
CELERY_RESULT_BACKEND=redis://redis:6379/0

# ─── MongoDB ──────────────────────────────────────────────────────────────────
# Internal Docker service hostname: mongodb
MONGODB_HOST=mongodb
MONGODB_PORT=27017
MONGODB_DB=repoguard_ai
MONGODB_USER=repoguard
MONGODB_PASSWORD=CHANGE_ME_STRONG_PASSWORD

# ─── Authentication ───────────────────────────────────────────────────────────
# REQUIRED: Generate with: openssl rand -hex 32
JWT_SECRET_KEY=CHANGE_ME_AT_LEAST_32_RANDOM_CHARACTERS
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE=15
REFRESH_TOKEN_EXPIRE=7
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=15
JWT_REFRESH_TOKEN_EXPIRE_DAYS=7
ACCESS_COOKIE_NAME=accessToken
REFRESH_COOKIE_NAME=refreshToken
# Set to true when serving over HTTPS
REFRESH_COOKIE_SECURE=false
REFRESH_COOKIE_SAMESITE=lax

# ─── CORS ─────────────────────────────────────────────────────────────────────
# Through Nginx the frontend calls /api on the same origin, so CORS is usually
# triggered. Set your production domain here if direct cross-origin calls occur.
CORS_ORIGINS=
CORS_ALLOWED_ORIGINS=
CORS_ALLOWED_ORIGIN_REGEX=

# ─── OpenAI / AI Provider ─────────────────────────────────────────────────────
LLM_PROVIDER=openai
OPENAI_API_KEY=
# Optional endpoint for OpenAI-compatible providers
OPENAI_BASE_URL=
OPENAI_MODEL=gpt-4o-mini
OPENAI_MAX_RETRIES=6
OPENAI_MIN_REQUEST_INTERVAL_SECONDS=1.5
PROBE_JUDGE_MAX_CONCURRENCY=1
PROBE_JUDGE_MAX_PROBES_PER_BATCH=8
PROBE_JUDGE_MAX_CHUNKS_PER_BATCH=24
PROBE_RETRIEVAL_MAX_CHUNKS=256
LLM_JOB_CALL_BUDGET=96
LLM_RATE_LIMIT_FAILURE_BUDGET=1
MISTRAL_API_KEY=
MISTRAL_BASE_URL=https://api.mistral.ai/v1
OPENROUTER_API_KEY=
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1

# ─── Embeddings / RAG ─────────────────────────────────────────────────────────
RAG_EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2
CODE_EMBEDDING_PROVIDER=mistral
CODE_EMBEDDING_MODEL=codestral-embed-2505
CODE_EMBEDDING_DIMENSION=1536
CODE_EMBEDDING_BATCH_SIZE=16
PROBE_SEMANTIC_QUERY_BATCH_SIZE=16
PROBE_SEMANTIC_MAX_QUERY_TOKENS=64
PROBE_DEFECT_MAX_CHUNKS=120
PROBE_COVERAGE_MAX_CHUNKS=24
PROBE_ROADMAP_MAX_CHUNKS=120
CODE_EMBEDDING_MAX_ITEM_TOKENS=1500
CODE_EMBEDDING_MAX_BATCH_TOKENS=12000
CODE_EMBEDDING_MAX_CONCURRENCY=2
CODE_EMBEDDING_MAX_PENDING_BATCHES=4
CODE_CHUNK_PARSE_CONCURRENCY=4
MONGODB_CHUNK_BATCH_SIZE=500
CODE_EMBEDDING_MAX_RETRIES=4
CODE_EMBEDDING_RETRY_BASE_DELAY_SECONDS=2.0
ENABLE_CODE_SEMANTIC_SEARCH=true

# ─── GitHub Bot (optional) ────────────────────────────────────────────────────
GITHUB_BOT_USERNAME=
GITHUB_BOT_TOKEN=
GITHUB_API_BASE_URL=https://api.github.com
GITHUB_API_VERSION=2026-03-10

# ─── Sandbox / Fix Executor ───────────────────────────────────────────────────
SANDBOX_TTL_HOURS=1
FIX_EXECUTOR_IMAGE=repoguard-fix-executor:latest
# Network mode for fix executor containers (none = no network access)
FIX_EXECUTOR_NETWORK=none
FIX_EXECUTOR_MEMORY_MB=1024
FIX_EXECUTOR_CPU_LIMIT=1.0
FIX_EXECUTOR_PIDS_LIMIT=256

# ─── Frontend ─────────────────────────────────────────────────────────────────
# Internal Docker service hostname for backend
API_PROXY_TARGET=http://backend:8000
NEXT_PUBLIC_API_URL=/api
NEXT_PUBLIC_API_BASE_URL=/api